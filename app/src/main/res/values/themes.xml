<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.AttendanceSystem" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.AttendanceSystem" parent="Base.Theme.AttendanceSystem" />


    <!--<style name="AppSplash" parent="Theme.SplashScreen" tools:ignore="NewApi">
        <item name="android:windowLightNavigationBar">@color/colorPrimary</item>
        <item name="android:windowLightStatusBar">@color/colorPrimary</item>
        <item name="android:windowBackground">@drawable/logo</item>

        <item name="android:windowSplashScreenAnimatedIcon">@drawable/logo_animation</item>
        <item name="android:windowSplashScreenBackground">@color/colorPrimary</item>
        <item name="android:windowSplashScreenAnimationDuration">1000</item>
        <item name="postSplashScreenTheme">@style/Theme.AttendanceSystem</item>
    </style>-->
</resources>